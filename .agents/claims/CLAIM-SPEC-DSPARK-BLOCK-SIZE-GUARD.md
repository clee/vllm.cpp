# CLAIM-SPEC-DSPARK-BLOCK-SIZE-GUARD

| Claim | Row IDs | Agent | Worktree / remote dir | Branch | Owned scope | State | Last update |
|---|---|---|---|---|---|---|---|
| `CLAIM-SPEC-DSPARK-BLOCK-SIZE-GUARD` | `SPEC-DSPARK-BLOCK-SIZE-GUARD` (`ACTIVE`) | Claude Code (opus-5), fresh implementer | isolated worktree, CPU-only build, no GPU lease and no publication | `row/DSPARK-BLOCK-SIZE-GUARD`, issue [#1225](https://github.com/mudler/vllm.cpp/issues/1225) | Owns ONLY the `ResolveDspark` ARGUMENTS of [`dspark-block-size-guard.md`](../specs/dspark-block-size-guard.md): the new `ReadDsparkDraftKeys` helper in `src/vllm/entrypoints/model_loader.cpp`, the `n_predict` / block-floor arguments at both `ResolveDspark` call sites (`:881-883` and `:1675-1677`), the narrowed k-required throw in the same branch, the `ResolveSpecConfig` access change in `include/vllm/entrypoints/model_loader.h`, and `tests/vllm/entrypoints/test_dspark_block_size_guard.cpp`. EXCLUDES `SpeculativeConfig::IsDsparkDraft`, `ResolveDsparkArchitecture` and the method/architecture classification in `ResolveSpecConfig`, all of which belong to `SPEC-DSPARK-QWEN3-ROUTING` ([#1193](https://github.com/mudler/vllm.cpp/issues/1193)) on a parallel branch; also excludes the DSpark speculator, the weight path, and the DeepSeek-V4 DSpark runtime | `ACTIVE` | 2026-08-18 — W1-W3 landed: red captured before the fix (8 cases, 4 failed), focused green (8/8, 22 assertions), G2 reachability mutation red with the call site reverted and the file restored byte-for-byte. G3 and G4 owed, both needing a GPU lease |
